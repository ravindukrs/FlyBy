# Example ~/.flyrc configuration file
# This shows the actual format that fly CLI uses and FlyBy expects

targets:
  my-ci:
    api: https://ci.example.com
    team: main
    token:
      type: bearer
      value: your-auth-token-here
    insecure: false
  
  dev-ci:
    api: https://dev-ci.example.com
    team: development
    token:
      type: bearer
      value: your-dev-token-here
    insecure: true
  
  prod-ci:
    api: https://prod-ci.example.com
    team: production
    token:
      type: bearer
      value: your-prod-token-here
    ca_cert: /path/to/ca.crt

# Notes:
# - The 'api' field contains the Concourse web interface URL
# - 'team' is the team you want to authenticate against
# - 'token' is a nested object with 'type' and 'value' fields
# - 'insecure' allows connections to servers with invalid/self-signed certificates
# - 'ca_cert', 'client_cert', and 'client_key' are for custom certificate authentication